export const metadata = {
  title: 'Find your Why',
  description: 'Find your won why and live your life on your terms',
};

export default function RootLayout({ children }) {
  return <div>{children}</div>;
}
